<!-- MainWindow.xaml -->
<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Loading Animation Style -->
        <Style x:Key="LoadingStyle" TargetType="Border">
            <Setter Property="Background" Value="#80000000"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Button Animation Style -->
        <Style x:Key="AnimatedButtonStyle" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Button.Click">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                             To="0.95" Duration="0:0:0.1" AutoReverse="True"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                             To="0.95" Duration="0:0:0.1" AutoReverse="True"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="{Binding SettingsMenuText}">
                <MenuItem Header="{Binding ChangeSettingsText}"/>
                <Separator/>
                <MenuItem Header="{Binding ExitText}"/>
            </MenuItem>
        </Menu>

        <!-- Team Selection Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <StackPanel Margin="10">
                <TextBlock Text="{Binding SelectTeamText}" FontWeight="Bold" Margin="0,0,0,5"/>
                <ComboBox Name="cmbSelectedTeam" Width="200" 
                          ItemsSource="{Binding Teams}" 
                          SelectedItem="{Binding SelectedTeam}"
                          SelectionChanged="CmbSelectedTeam_SelectionChanged"/>
            </StackPanel>

            <StackPanel Margin="10">
                <TextBlock Text="{Binding SelectOpponentText}" FontWeight="Bold" Margin="0,0,0,5"/>
                <ComboBox Name="cmbSelectedOpponent" Width="200"
                          ItemsSource="{Binding Opponents}"
                          SelectedItem="{Binding SelectedOpponent}"
                          SelectionChanged="CmbSelectedOpponent_SelectionChanged"/>
            </StackPanel>

            <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Name="lblResult" 
                           Text="" 
                           FontSize="20" FontWeight="Bold" 
                           Foreground="Green" HorizontalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <!-- Team Info Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,60,0,10">
            <Button Name="btnSelectedTeamInfo" Content="{Binding SelectedTeamInfoText}" 
                    Width="150" Height="30" Margin="5"
                    Style="{StaticResource AnimatedButtonStyle}"
                    IsEnabled="{Binding HasSelectedTeam}"/>
            <Button Name="btnOpponentTeamInfo" Content="{Binding OpponentTeamInfoText}" 
                    Width="150" Height="30" Margin="5"
                    Style="{StaticResource AnimatedButtonStyle}"
                    IsEnabled="{Binding HasSelectedOpponent}"/>
        </StackPanel>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Grid Name="footballField" Margin="20">
                <Grid.Background>
                    <ImageBrush ImageSource="Resources/football_field.png" Stretch="Fill"/>
                </Grid.Background>
                <!-- Field visualization will be populated programmatically -->
                <Canvas Name="fieldCanvas" ClipToBounds="True"/>
            </Grid>
        </ScrollViewer>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding ChampionshipText}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4" Style="{StaticResource LoadingStyle}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="10"/>
                <TextBlock Text="{Binding LoadingText}" Foreground="White" 
                           HorizontalAlignment="Center" FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
